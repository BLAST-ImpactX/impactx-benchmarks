"""2.5D space-charge scenario parameters.

Uses the IDENTICAL beam, grid and drift as the 3D space-charge benchmark, so the only
difference between the two benchmarks is the solver dimensionality (3D vs 2.5D).
"""
from scenarios.spacecharge.params import PARAMS  # noqa: F401
